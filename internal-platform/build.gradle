plugins {
    id("java-platform")
}

apply(from: "$rootDir/gradle/dependency-versions.gradle")

dependencies {
    api(platform("org.junit:junit-bom:$libraryVersions.junit"))
}

javaPlatform {
    allowDependencies()
}
